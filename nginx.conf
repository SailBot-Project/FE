# nginx.conf
server {
    listen 80;
    server_name 54.180.152.40;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

}